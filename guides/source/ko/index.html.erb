<% content_for :page_title do %>
루비 온 레일스 가이드
<% end %>

<% content_for :header_section do %>
<%= render 'welcome' %>
<% end %>

<% content_for :index_section do %>
<div id="subCol">
  <dl>
    <dt></dt>
    <dd class="kindle">레일스 가이드는 <%= link_to '킨들', @mobi %>에서도 읽을 수 있습니다.</dd>
    <dd class="work-in-progress">이 아이콘으로 표시한 가이드는 현재 작성하고 있으며 가이드 목차에서 보이지 않을 것입니다. 가이드의 내용은 좋지만 불완전한 정보와 에러가 있을 수 있습니다. 내용을 검토후 코멘트와 오류 수정내용을 남겨서 도울 수 있습니다.</dd>
  </dl>
</div>
<% end %>

<% documents_by_section.each do |section| %>
  <h3><%= section['name'] %></h3>
  <dl>
    <% section['documents'].each do |document| %>
      <%= guide(document['name'], document['url'], work_in_progress: document['work_in_progress']) do %>
        <p><%= document['description'] %></p>
      <% end %>
    <% end %>
  </dl>
<% end %>
