<% content_for :page_title, "Ruby on Rails Guides" %>
<% content_for :description, "Ruby on Rails Guides" %>

<% content_for :header_section do %>
<%= render 'welcome' %>
<% end %>

<% content_for :index_section do %>
<div id="subCol">
  <dl>
    <dt></dt>
    <% unless @edge -%>
      <dd class="kindle">레일스 가이드는 <%= link_to 'Kindle', @mobi %>로도 볼 수 있다.</dd>
    <% end -%>
    <dd class="work-in-progress">이 아이콘으로 표시된 가이드 내용은 현재 작업 중이며 가이드 인덱스 메뉴에서 볼 수 없다. 여전히 유용하지만 불완전한 정보와 오류까지 포함 할 수 있습니다. 가이드 내용을 검토한 후 댓글과 수정 사항을 보내 주면 도움이 될 수 있다.</dd>
  </dl>
</div>
<% end %>

<% documents_by_section.each do |section| %>
  <h3><%= section['name'] %></h3>
  <dl>
    <% section['documents'].each do |document| %>
      <%= guide(document['name'], document['url'], work_in_progress: document['work_in_progress']) do %>
        <p><%= document['description'] %></p>
      <% end %>
    <% end %>
  </dl>
<% end %>
